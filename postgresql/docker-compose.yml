services:
  postgresql:
    image: postgres:latest
    container_name: postgresql
    environment:
      POSTGRES_USER: "postgres"
      POSTGRES_PASSWORD: "TuContraseña123*"
    ports:
      - "5432:5432"
    volumes:
      - postgresql:/var/lib/postgresql

volumes:
  postgresql:

