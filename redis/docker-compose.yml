version: '3.8'
services:
  redis:
    image: redis:latest
    container_name: redis
    command:
      - redis-server
      - --requirepass
      - "TuContraseña123*"
      - --appendonly
      - "yes"
      - --dir
      - /data
    ports:
      - "6379:6379"
    volumes:
      - redis:/data
    restart: no

volumes:
  redis:
