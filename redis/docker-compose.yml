services:
  redis:
    image: redis:latest
    container_name: redis
    command:
      - redis-server
      - --requirepass
      - "TuContraseña123*"
      - --appendonly
      - "yes"
      - --dir
      - /data
    ports:
      - "6379:6379"
    volumes:
      - redis:/data

volumes:
  redis:
